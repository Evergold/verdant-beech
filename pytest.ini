[pytest]
asyncio_mode = auto
markers =
    tier1: Fast, deterministic evaluations (e.g. Prompt Assembly, RAG retrieval) that should run on every commit.
    tier2: Slow, expensive evaluations (e.g. Multimodal E2E, LLM-as-a-judge) to run nightly.
