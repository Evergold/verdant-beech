{
  "name": "verdant-beech",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "vite",
    "dev:backend": "PYTHONPATH=. server/.venv/bin/uvicorn server.main:app --reload --port 8001",
    "start": "PYTHONPATH=. server/.venv/bin/uvicorn server.main:app --port 8001",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^10.0.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@babylonjs/core": "^9.17.0",
    "@babylonjs/materials": "^9.17.0",
    "i18next": "^23.7.11"
  }
}
